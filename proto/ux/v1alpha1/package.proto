edition = "2024";

package ux.v1alpha1;

import "ux/v1alpha1/nix.proto";

message Package {
  string name = 1;
  oneof source {
    Derivation derivation = 10;
  }
  oneof destination {
    string relative_path = 20;
  }
}
