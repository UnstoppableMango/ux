edition = "2024";

package ux.v1alpha1;

import "google/protobuf/any.proto";
import "ux/v1alpha1/nix.proto";

message Config {
  repeated Repo repos = 1;
  repeated Link links = 2;
  map<string, string> builders = 3;
  map<string, Generate> generate = 4;
}

message Generate {
  string builder = 1;
  google.protobuf.Any config = 2;
}

message FlakeRepo {
  string url = 1;
}

message GitRepo {
  string url = 1;
  string rev = 2;
}

message OciRepo {
  string url = 1;
}

message Repo {
  string name = 1;
  oneof src {
    FlakeRepo flake = 10;
    GitRepo git = 11;
    OciRepo oci = 12;
  }
}

message Destination {
  string relative_path = 1;
}

message Link {
  string name = 1;
  Destination destination = 2;
  oneof source {
    Derivation derivation = 100;
  }
}
